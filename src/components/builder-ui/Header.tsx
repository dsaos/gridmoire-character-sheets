import styles from './Header.module.scss';

const Header = () => {
  return (
    <header className={styles.header}>
      <h2>Gridmoire</h2>
      <h1>Default Sheet</h1>
    </header>
  )
};

export default Header;
